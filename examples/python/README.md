# Python Examples

This repo is structured for both C++ and Python examples.

- `examples/cpp/` contains the initial migrated C++ apps.
- `examples/python/` is reserved for future Python examples, organized by the same task categories.

Python examples are intentionally not wired into the CMake build and are added only when implemented.
