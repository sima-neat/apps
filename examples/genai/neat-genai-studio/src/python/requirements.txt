PyYAML
Flask>=3,<4
Flask-SocketIO>=5,<6
Flask-Cors>=6,<7
requests>=2,<3
huggingface_hub[cli]>=0.34.0,<1.0
# --- Text-to-speech (main venv) -----------------------------------------
# piper-plus: MIT Piper fork with custom G2P; owns the top-level `piper`
# package and adds Japanese (ja) plus en/zh/es/fr/pt. Runs on onnxruntime; it
# pulls its own phonemizers (pyopenjtalk-plus for JA, g2p-en, pypinyin).
#
# NOTE: piper-tts also ships a top-level `piper` package and CANNOT be installed
# in this venv without clobbering piper-plus. It is installed in a separate venv
# (.venv-pipertts, see requirements-pipertts.txt) and reached via a subprocess
# worker (pipertts_worker.py) for the German/Italian/Norwegian/Vietnamese voices.
piper-plus>=1.13.0
