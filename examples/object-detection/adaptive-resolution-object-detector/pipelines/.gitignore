# Generated by pipeline.py on every `up` - never a source file.
*-run.yaml
# Per-run logs from the detectors and the UI servers.
logs/
*.log
# Live UI state: rewritten as streams are added/removed.
ui-state.json
# setup-adaptive-pipeline.sh backups.
*.bak-setup
__pycache__/
*.pyc
# UI language toggle (python|cpp) - user state, not source.
language
