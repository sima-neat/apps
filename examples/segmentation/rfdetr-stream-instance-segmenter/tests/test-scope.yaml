models:
  rfdetr-seg-432-base:
    source: url
    # TODO: replace with the real hosted archive once rfdetr_seg_432_base.tar.gz is published
    # (see README.md "Prepare the Model"). Placeholder so CI does not attempt to fetch it while
    # e2e coverage below stays disabled.
    url: https://example.invalid/rfdetr-seg-432/rfdetr_seg_432_base.tar.gz
    file: rfdetr_seg_432_base.tar.gz
unit:
  python: true
  cpp: true
e2e:
  python:
    enabled: false
    reason: >-
      Model archive is not hosted yet (see README.md TODO under "Prepare the Model").
      Enable once rfdetr_seg_432_base.tar.gz has a real download URL and an RTSP test
      fixture is available.
    models: []
  cpp:
    enabled: false
    reason: >-
      Model archive is not hosted yet (see README.md TODO under "Prepare the Model").
      Enable once rfdetr_seg_432_base.tar.gz has a real download URL and an RTSP test
      fixture is available.
    models: []
